
/** This object has naturalHeight and naturalWidth. */
/** Represents and object dimensions expressed as naturalHeight and naturalWidth in pixels. */
export type HasNaturalDimensions = {
	/** pixels on the y-axis */
	naturalHeight: number;

	/** pixels on the x-axis */
	naturalWidth: number;
};
