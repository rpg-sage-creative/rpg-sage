
/** Represents and object dimensions expressed as height and width in pixels. */
export type HasPixelDimensions = {
	/** height in pixels */
	height: number;

	/** width in pixels */
	width: number;
};
