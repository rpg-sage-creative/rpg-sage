import type { MapCache } from "../cache/MapCache.js";
import type { GridArgs } from "../types/GridArgs.js";

/** @internal */
export async function renderGrid(mapCache: MapCache, gridArgs: GridArgs): Promise<void> {
	mapCache;
	gridArgs;
}
