export enum SnowflakeType { ChannelReference, CustomEmoji, RoleMention, UserMention, Snowflake }

export enum MessageType { Both = 0, Post = 1, Edit = 2 }

export enum ReactionType { Both = 0, Add = 1, Remove = 2 }
