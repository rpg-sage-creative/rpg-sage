import { error } from "@rsc-utils/console-utils";
import { runTests } from "@rsc-utils/test-utils";

// node --experimental-modules --es-module-specifier-resolution=node run.mjs

runTests(async function testRpgSageLegacy() {
	error(`Create rpg-sage-legacy tests`);
}, true);
